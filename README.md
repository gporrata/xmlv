# xmlv

A simple rust cli for viewing xml files similar to how `jless` views json. I should have called it `xmlless` but took too long to type.

## Usage

```bash

cat test.xml | xml
# or
xml test.xml
```
